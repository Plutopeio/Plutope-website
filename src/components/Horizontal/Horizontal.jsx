import { Container } from "@mui/material";
import React from "react";

const Horizontal = () => {
  return (
    <Container className="container horizontal_container">
      
    </Container>
  );
};

export default Horizontal;
